# Declare all source files in the SOURCES variable, with "src" as the relative root.
# Write source files names on one line, or on multiple lines by adding a backslash at the end and going on a new line (no other characters after the backslash).
SOURCES := \
main.c
