# Declare all sources files in the SOURCES variable, without "src\" prefix.
# Write source files names on one line separated by a space, or on multiple lines by adding a backslash at the end and going on a new line (no other characters after the backslash).
SOURCES := \
main.c
